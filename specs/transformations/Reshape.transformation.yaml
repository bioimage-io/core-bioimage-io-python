name: Reshape
description: >
    reshape array(s)
    Target shape allows for special values:
        '-1': max one occurrence; expands to fitting size
        '-2': ignore axes, dimension size remains unchanged+(

cite: []
authors:
    - Fynn Beuttenmueller
documentation: transformations.md
tags: [reshape]
license: MIT

format_version: 0.1.0
language: python

dependencies: ../../env_numpy.yaml

source: pybio.core.transformations.reshape.Reshape
required_kwargs: [shape]
optional_kwargs: {apply_to: null}

inputs: dynamic
outputs: dynamic
