name: Reshape
description: applies numpy.reshape, while also allowing for '-2' values. In this case those axes are left unchanged
cite: []
authors:
    - Fynn Beuttenmueller
documentation: transformations.md
tags: [reshape]

format_version: 0.1.0
language: python

dependencies: ../../env_numpy.yaml

source: pybio.transformations.reshape.Reshape
required_kwargs: [shape]

inputs: dynamic
outputs: dynamic
