name: NumpyReshape
description: applies numpy.transpose
cite: []
authors:
    - Fynn Beuttenmueller
documentation: transformations.md
tags: [numpy, reshape]

format_version: 0.1.0
language: python

dependencies: ../../env_numpy.yaml

source: pybio.transformations.numpylike.Transpose
optional_kwargs: {axes: null}

inputs: any
outputs: same # that's a lie! need outputs with input references, while inputs is any... todo: a lot inputs/outputs
